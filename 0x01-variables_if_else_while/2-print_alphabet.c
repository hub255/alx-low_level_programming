#include <stdio.h>
/**
*main - Prints the alphabet in lowercase.
*
*Description: Refers to the main
* Return: Always 0.
*/
int main(void)
{
char y;
for (y = 'a'; y <= 'z'; y++)
putchar(y);
putchar('\n');
return (0);
}
